table:
  name: locations
  schema: db
array_relationships:
  - name: crashes
    using:
      foreign_key_constraint_on:
        column: location_id
        table:
          name: crashes
          schema: db
