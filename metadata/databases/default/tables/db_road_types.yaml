table:
  name: road_types
  schema: db
array_relationships:
  - name: crashes
    using:
      foreign_key_constraint_on:
        column: road_type_id
        table:
          name: crashes
          schema: db
  - name: crashes_cris
    using:
      foreign_key_constraint_on:
        column: road_type_id
        table:
          name: crashes_cris
          schema: db
  - name: crashes_vzs
    using:
      foreign_key_constraint_on:
        column: road_type_id
        table:
          name: crashes_vz
          schema: db
